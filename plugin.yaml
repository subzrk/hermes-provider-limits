name: provider-limits
version: 1.1.0
description: Live provider quotas and per-session usage for Hermes Desktop.
author: Jaime Marques
homepage: https://github.com/JaimeMarques/hermes-provider-limits
# Backend config_effective, profile-scoped routes, and provider credential APIs
# require v2026.9.14 (Hermes 0.21.3). Desktop SDK/package discovery requirements
# are older. dashboard/plugin_api.py also guards loaders ignoring this field.
requires_hermes: ">=0.21.3"
